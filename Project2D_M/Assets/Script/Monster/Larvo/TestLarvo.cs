using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Spine.Unity;

public class TestLarvo : MonoBehaviour
{

	SpineAnimCollider m_anim_Test;
	SkeletonDataAsset m_Data_test;

    // Start is called before the first frame update
    void Start()
    {
		m_anim_Test = GetComponent<SpineAnimCollider>();
    }

    // Update is called once per frame
    void Update()
    {
		//m_Data_test.
    }
}
